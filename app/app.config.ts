// Maps Nuxt UI's semantic color slots onto the brand palette defined in assets/css/main.css.
export default defineAppConfig({
  ui: {
    colors: {
      primary: "brand",
      neutral: "slate",
    },
  },
});
