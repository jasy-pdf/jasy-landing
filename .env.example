# Token for scripts/release.sh to push the release commit + tag.
# Fine-grained PAT scoped to jasy-pdf/jasy-landing with "Contents: Read and write",
# or a classic PAT with the "repo" scope.
# Optional — without it, release.sh falls back to `git push origin` (your normal git auth).
GH_TOKEN=
